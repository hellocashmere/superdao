/**
 * Exposes the props accepted by the shared summary metric card.
 */
export type { MetricCardProps } from "./metric-card";

/**
 * Exposes the shared summary metric card.
 */
export { MetricCard } from "./metric-card";
